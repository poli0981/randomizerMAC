name: Notify CI Failures

on:
  workflow_run:
    workflows: ["*"]
    types: [completed]
    
permissions:
  contents: read
  actions: read

jobs:
  notify:
    uses: poli0981/.github/.github/workflows/notify-ci-failure.yml@main
    secrets: inherit
